export type Entry = {
	id: number;
	name: string;
	sku: string;
	createdAt: string;
	createdById: string;
};


export type Exit = {
	id: number;
	name: string;
	sku: string;
	createdAt: string;
	createdById: string;
};

